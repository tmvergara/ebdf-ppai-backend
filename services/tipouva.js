class TipoUva {
    constructor(nombre, descripcion) {
        this.nombre = nombre,
        this.descripcion = descripcion
    }
    esTipoUva(nombre) {
        if (this.nombre = nombre) {
            return true
        }
    }
} 

module.exports = TipoUva;